{% extends 'base.html-back.twig' %}

{% block title %}Cheval{% endblock %}

{% block body %}
<a href="{{ path('app_admin_cheval_index') }}" class="btn btn-primary">Retour à la liste</a>
    <a href="{{ path('app_admin_cheval_edit', {'id': cheval.id}) }}" class="btn btn-sucess">Modifier</a>
    <h1>Cheval</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cheval.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cheval.nom }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ cheval.lieu }}</td>
            </tr>
            <tr>
                <th>Alimentation</th>
                <td>{{ cheval.alimentation }}</td>
            </tr>
            <tr>
                <th>Veto</th>
                <td>{{ cheval.veto }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ cheval.imageName }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ cheval.updatedAt ? cheval.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    

    {{ include('admin_cheval/_delete_form.html.twig') }}
{% endblock %}
