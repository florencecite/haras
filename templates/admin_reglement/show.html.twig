{% extends 'base.html-back.twig' %}


{% block title %}Reglement{% endblock %}

{% block body %}
<a href="{{ path('app_admin_reglement_index') }}"class="btn btn-primary">Retour à la liste</a>
<a href="{{ path('app_admin_reglement_edit', {'id': reglement.id}) }}"class="btn btn-success">Modifier</a>
    <h1>Reglement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reglement.id }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ reglement.texte }}</td>
            </tr>
        </tbody>
    </table>

    

    {{ include('admin_reglement/_delete_form.html.twig') }}
{% endblock %}
