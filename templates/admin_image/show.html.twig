{% extends 'base-backend.html.twig' %}


{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ image.imageName }}</td>
            </tr>
            <tr>
                <th>ImageFile</th>
                <td>{{ image.imageFile }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ image.updatedAt ? image.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_image_index') }}" class="btn btn-primary">Retour à la liste</a>
    <a href="{{ path('app_admin_image_edit', {'id': image.id}) }}"class="btn btn-success">Modifier</a>

    {{ include('admin_image/_delete_form.html.twig') }}
{% endblock %}
