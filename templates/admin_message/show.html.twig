{% extends 'base-backend.html.twig' %}
{% block title %}Message{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_message_index') }}"class="btn btn-primary">Retour à la liste</a>
    <a href="{{ path('app_admin_message_edit', {'id': message.id}) }}"class="btn btn-success">Modifier</a>
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Descriptif</th>
                <td>{{ message.descriptif }}</td>
            </tr>
        </tbody>
    </table>

    

    {{ include('admin_message/_delete_form.html.twig') }}
{% endblock %}
