{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
<a href="{{ path('app_front_cheval_index', {'person_id': user.id}) }}" class="btn btn-dark">Retour à la liste</a>       
    <h1>Modifier</h1>

    
    
    {{ include('front_cheval/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('front_cheval/_delete_form.html.twig') }}
{% endblock %}
