<form method="post" action="{{ path('app_admin_evenement_sante_delete', {'id': evenement_sante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement_sante.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
{# <i class="align middle" data-feather="trash-2"> #}
