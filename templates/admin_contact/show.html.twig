{% extends 'base-backend.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
<a href="{{ path('app_admin_contact_index') }}" class="btn btn-primary">Retour à la liste</a>
<a href="{{ path('app_admin_contact_edit', {'id': contact.id}) }}"class="btn btn-success">Modifier</a>
    <h1>Contact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ contact.prenom }}</td>
            </tr>
            <tr>
                <th>NumeroTelephone</th>
                <td>{{ contact.numeroTelephone }}</td>
            </tr>
        </tbody>
    </table>

    

    {{ include('admin_contact/_delete_form.html.twig') }}
{% endblock %}
